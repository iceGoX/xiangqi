# 中国象棋

本目录是独立中国象棋项目，发布子路径 `/xiangqi/`。先阅读 `rule.md`、`README.md`，检查 Git 状态；只在本目录操作，不修改相邻网站。

- 视觉以 Pixso 可编辑画板为基准，原木、水墨、宣纸、柔和青绿按钮；红色采用用户确认的 `#B84A46`，保留朱红感，只略降饱和度，不偏棕。
- 在线与同屏使用同一个 `shared/engine.js`。在线以服务端校验为准，使用玩家令牌、版本检查、幂等请求和 SSE。
- 按 `rule.md` 保留七种走法，允许不解将、送将和照面；只有实际吃将才获胜并进入结算画面，禁止恢复将死、困毙或长将自动判负。
- 合法走棋平滑移动，非法走棋原地抖两次；动画锁定、断线恢复和减少动态效果必须保留。
- 真实主机、部署地址、私钥、令牌、Pixso 文件索引只放本地忽略目录 `.local/`；服务器部署采用静态白名单和同一构建产物校验，不部署原图、设计源、测试或存档；源码仓库保留代码、测试、通用说明和压缩运行素材。
- 验证 `npm test`、`npm run check`、`npm run build`，并在浏览器检查电脑、平板、手机和在线双端。不能把自动测试当作真机验证。
- 未经明确要求不提交或推送。部署当前任务已获授权，保留回滚能力，不覆盖其他网站或房间存档。
- 文本、代码末尾不增加空行。
