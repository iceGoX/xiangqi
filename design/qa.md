# 当前版本验收

版本 `17a7485bad0e2f09`，2026-09-09。

- 17 张运行图片由 JPG／透明 PNG 转成 WebP，2,053,923 字节降到 381,906 字节，减少 81.4%。已检查桌面和手机压缩后截图，木纹与棋子透明边缘正常。
- 棋盘及 14 类棋子提前加载、解码后再展示；图片 URL 携带版本参数，长缓存一年。页面及脚本使用 no-cache 重新校验。
- 放开不解将、送将、将帅照面，保留基本走法；移除将死、困毙、长将及循环自动裁判。只有真实吃将产生胜者，主动结束／退出没有胜者。
- 26 项规则、在线服务、动画定位与启动测试通过。服务端测试覆盖不解将后的实际吃将。
- 本地与公网各从标准开局以桌面及手机触控浏览器走完 13 手，验证被将军后仍可走其他棋，吃将前无结算，吃将后双方出现胜利弹窗；再来一局和退出正常。
- 同屏模式以黑方吃帅验证另一方获胜分支，并验证平板／手机尺寸下重开、图片解码及无横向溢出。
- 公网全部公开文件哈希与本地构建一致，WebP MIME、图片长缓存和脚本重新校验响应头通过。

手机和平板测试为浏览器触控模拟，未做实体设备验收。完整自定义规则见 `rule.md`。

## 结算画面修订

版本 `1e1f79f03237b28e`：获胜后进入独立结算页面，采用宣纸边框、木质胜方棋子、胜印、大标题和本局统计。桌面 1440 × 1100 与触控手机 390 × 844 实际截图已检查；标准开局 13 手吃将后双方进入结算页面，可直接协商再来一局并恢复棋盘。

平板 834 × 1194 触控检查、查看棋局及同屏再来一局通过；公网桌面／手机吃将后的完整结算及双方协商再来一局通过，公开版本与三个修改文件哈希一致。
